/**
 * フロントエンド側の薄い呼び出しラッパ。
 *
 * 所有: `IpcClient`（design.md「Components and Interfaces → Frontend Layer」）。
 * 要件: 4.1（単一の通信境界）, 4.2（境界を越える型をひとつの定義から得ること）,
 * 4.4（失敗の原因を識別でき、成功と区別できること）。下流ではタスク 2.4 が要件 4.1・4.2・4.4 を
 * 担う。
 *
 * 本ファイルはタスク 1.4 が置いた骨組みである。**振る舞いを持たない。**実体を埋めるのは
 * タスク 2.4 であり、そのとき次をここへ実装する:
 *
 * - コマンド名は `./bindings`（ts-rs の生成物）が持つ定数を参照する。**文字列リテラルを手で書かない。**
 * - 戻り値は生成物の `IpcResult` 判別可能な合併型とし、`status` で網羅的に分岐できるようにする。
 *
 * 依存の向き（design.md の確定事項）: `./bindings.ts` はタスク 2.2 が生成する追跡対象の
 * 生成物であり、**手で編集しない**（structure.md「生成物は追跡するが手で編集しない」）。
 * タスク 2.2 が生成するまで本ディレクトリに `bindings.ts` を置かない。
 */
export {};
