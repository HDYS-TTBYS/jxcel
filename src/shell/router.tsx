/**
 * 画面間の遷移を扱う単一の仕組み。
 *
 * 所有: `ShellLayout` の遷移部分（design.md「Components and Interfaces → Frontend Layer」）。
 * 要件: 9.2（画面間の遷移を単一の仕組みで扱い、現在表示されている画面を識別できること）。
 *
 * 本ファイルはタスク 1.4 が置いた骨組みである。**振る舞いを持たない。**実体を埋めるのは
 * タスク 9.1 であり、そのとき画面レジストリと現在画面の表現をここへ実装し、
 * `Layout`（[`./Layout`]）が選択された画面を領域へ差し込む。
 *
 * 実装上の制約（design.md・structure.md より）: 遷移の仕組みは 1 つだけ置く。個別機能が
 * 独自のルーティングを持ったら誤りである（要件 9.2）。
 */
export {};
