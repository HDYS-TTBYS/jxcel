/**
 * 画面単位のエラー隔離 — 個別機能の画面が描画中に失敗してもアプリ全体を停止させない境界。
 *
 * 所有: `ShellLayout` のエラー隔離部分（design.md「Components and Interfaces → Frontend Layer」）。
 * 要件: 9.5（画面の描画中のエラーでアプリ全体を停止させず、該当する領域にエラーを提示すること）。
 *
 * 本ファイルはタスク 1.4 が置いた骨組みである。**振る舞いを持たない。**実体を埋めるのは
 * タスク 9.3 であり、そのとき React のエラー境界として、該当する領域だけにエラーを提示する
 * 実装をここへ置く。
 *
 * 実装上の制約（design.md より）: 隔離の粒度は**画面単位**である。配置先は `Layout.tsx` の
 * `ShellRegion` が `<Screen screenId={screen.id} navigate={navigate} />` を描いている 1 式であり、
 * **その式だけを包む**（クロームや領域そのものを包むとアプリ全体が止まる）。
 */
export {};
