/**
 * 3 OS の描画確認に使う、多数の要素を持つ表形式の最小画面。
 *
 * 所有: `SmokeScreens`（design.md「Components and Interfaces → Frontend Layer」）。
 * 要件: 10.4（多数の要素を持つ表形式の描画と文字編集を伴う描画のそれぞれについて、
 * 3 つの OS で成立することを確認できる最小の画面を提供すること）。
 *
 * 本ファイルはタスク 1.4 が置いた骨組みである。**振る舞いを持たない。実用画面でもない。**
 * 実体を埋めるのはタスク 9.7 であり、そのとき多数行の表を描画し、**描画フレームの中から
 * 到達の通知を送る発信側**をここに置く（監視側は受け取るだけであり、発信はフロントエンドの
 * 責任である）。実用水準へ育てるのは `data-grid` スペックである。
 */
export {};
