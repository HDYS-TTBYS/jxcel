//! 補助プロセスのホスト — プラットフォーム別のパス解決と `SidecarSupervisor` の束ね、
//! および補助プロセスの出力を診断の記録先へ流す購読の結線。
//!
//! 所有: `SidecarHost`（design.md「Components and Interfaces → Adapter Layer」）。
//! 要件: 5.1, 5.2, 5.3, 5.5, 5.9。
//!
//! 本ファイルはタスク 1.3 が置いた空のモジュール骨組みである。実体を埋めるのはタスク 8.1。
//! 前提となる他のタスク:
//!
//! - タスク 1.7: 同梱原本の配置規約と 3 OS のバンドル配置。
//! - タスク 3.1〜3.5: 整合性検査・起動と共有・終了保証・出力取得・孤児掃除
//!   （`crates/app-shell/` の `sidecar`）。
//!
//! 実装上の制約（design.md より）: 配置はプラットフォームごとに異なる。Windows と macOS は
//! 標準の同梱機構を使い、**Linux は標準の同梱機構を使えない**（バンドル処理が `usr/bin` 配下の
//! 実行ファイルを無条件に書き換えるため）ので `usr/share/jxcel/` に置いて走査対象の外へ出す。
//! 整合性検査は必ず起動の前に通す。**フロントエンドにプロセス起動の経路を与えない**ため、
//! 起動は常に Rust 側から行う（要件 4.7）。
