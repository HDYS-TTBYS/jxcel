//! 初回描画の監視とラスタライザの判定 — ウィンドウ生成時に期限付きの監視を開始し、
//! フロントエンドが描画フレーム内から通知する。期限超過時はソフトウェアラスタライズか
//! どうかを判定して記録する。
//!
//! 所有: `RenderWatchdog`（design.md「Components and Interfaces → Adapter Layer」）。
//! 要件: 10.1, 10.2, 10.3。
//!
//! 本ファイルはタスク 1.3 が置いた空のモジュール骨組みである。実体を埋めるのは:
//!
//! - タスク 8.2: ハートビートと判定（要件 10.1, 10.2）。判定は `Painted` / `SoftwareRaster` /
//!   `NoPaint` の三値で表す。
//! - タスク 8.3: 代替経路の次回起動時の適用（要件 10.3）。
//!
//! 実装上の制約（design.md と決定 7 より）: **描画失敗を検出する API は基盤側に存在しない。**
//! 空白の画面と資産の読み込み失敗は外からは区別できないため、この経路が唯一の判別手段になる。
//! 回避策の環境変数は描画基盤の初期化前に設定する必要があり検出時点では手遅れであるため、
//! `NoPaint` を検出したら設定に印を残し、**次回起動時**に `AppLifecycle`（[`crate::lifecycle`]）
//! が `Builder` の前で読んで適用する。**無条件には適用しない**。
