//! 大きなペイロードの経路 — `tauri::ipc::Response` の生バイトで応答し、JSON を通さない。
//!
//! 所有: 一括転送のコマンド（design.md「File Structure Plan」の `commands/bulk.rs`）。
//! 要件: 4.5。
//!
//! 本ファイルはタスク 1.3 が置いた空のモジュール骨組みである。実体を埋めるのはタスク 7.2。
//! 実装上の制約（research.md「IPC の転送形式と大きなペイロード」より）:
//!
//! - 応答の内容型が JSON でもテキストでもない場合、フロントエンドには `ArrayBuffer` として
//!   届く。**行ごとに境界を越えることを必要としない形にする。**
//! - 引数側で生バイトを送る場合、バッファは**引数全体**でなければならない。入れ子にすると
//!   数値の配列へ変換される。
//! - `Channel` は一括転送に向かない（閾値を超えるメッセージは追加の往復を生む）。
