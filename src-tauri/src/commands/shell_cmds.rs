//! app-shell 自身のコマンド — ドメインクレートを呼ばず、シェルの機構（設定・診断・
//! ウィンドウ・描画の通知）だけを扱うコマンドを置く。
//!
//! 所有: app-shell 自身のコマンド面（design.md「File Structure Plan」の `commands/shell_cmds.rs`）。
//! 要件: 4.1, 4.4, 4.6, 7.4。
//!
//! 本ファイルはタスク 1.3 が置いた空のモジュール骨組みである。根への列挙と共通の結線は
//! タスク 7.1 が行い（要件 4.1, 4.4, 4.6）、個々のコマンドはそれを必要とするタスクが追加する
//! （例: 設定変更の通知はタスク 7.1、描画の通知はタスク 8.2）。
//!
//! 規約: すべてのコマンドは `IpcResult` を返し、呼び出し元ウィンドウを引数として受け取る。
//! ただし描画の通知のようにウィンドウ文脈を必要としないものはこの限りではない（タスク 8.2）。
