//! ウィンドウの生成とレジストリ — ラベル規約 `doc-<連番>` / `empty-<連番>` と、ウィンドウの
//! 識別子から状態への写像を保持する。ウィンドウ単位の状態管理は基盤側に存在しない。
//!
//! 所有: `WindowManager`（design.md「Components and Interfaces → Adapter Layer」）。
//! 要件: 2.1, 2.2, 2.3, 2.5, 2.7, 2.10, 4.6。
//!
//! 本ファイルはタスク 1.3 が置いた空のモジュール骨組みである。実体を埋めるのは:
//!
//! - タスク 6.1: 生成（**非同期**。同期コマンドおよびイベントハンドラ内での生成は Windows で
//!   デッドロックする）とレジストリ。
//! - 子モジュール [`close`]（終了拒否の仲介 / タスク 7.6）と [`geometry`]（位置とサイズの
//!   記憶 / タスク 6.3）。
//!
//! `tauri.conf.json` の `app.windows` が宣言する起動時の 1 枚はタスク 1.3 が置いた暫定であり、
//! ラベル規約と生成経路はタスク 6.1 が引き取る。

mod close;
mod geometry;
