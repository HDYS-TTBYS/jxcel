{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "既定の中核権限集合のみを与える capability。ファイルシステム系（fs）とシェル系（shell）の権限を意図的に含めない（要件 4.7、design.md「Security Considerations」）。フロントエンドから任意のファイルやプロセスへ到達する経路を作らないための第一の制御であり、依存に該当プラグインが無いことと対になっている。生成物 src-tauri/gen/schemas/capabilities.json に対する機械検査はタスク 7.3 が追加する。",
  "permissions": ["core:default"]
}
