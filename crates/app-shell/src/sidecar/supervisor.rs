//! 補助プロセスの起動・共有・再起動・終了・出力の取得（要件 5.4〜5.9）。
//!
//! 種類ごとに高々 1 つのプロセスを保持し、要求が重なっても起動を 1 回に収める（要件 5.5）。
//! 予期せず終了していた場合は次に必要になった時点で改めて起動を試みる（要件 5.8）。起動失敗は
//! 原因を区別できる列挙として返す（要件 5.4）。終了保証は Tauri のプラグインに委ねず、
//! Windows は Job Object、Unix はプロセスグループで自前に行う（要件 5.6、research.md 決定 5）。
//!
//! 本タスク（1.2）では骨組みのみを置く。実体は tasks.md 3.2 が追加する。
