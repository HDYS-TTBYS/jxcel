//! コマンド名の単一配列（要件 4.1、4.2）。
//!
//! `src-tauri` のハンドラ登録と TypeScript の生成物（`src/ipc/bindings.ts`）が**同じ配列**を
//! 参照することで、名前のドリフトを構造的に塞ぐ。この配列に現れない名前でコマンドを登録しては
//! ならない（design.md「IpcContract」の不変条件）。呼び出し側が文字列リテラルを書くことも
//! 禁じる（同「Implementation Notes」）。
//!
//! 本タスク（1.2）では骨組みのみを置く。定数配列の定義と生成の入口は tasks.md 2.2 が追加する。
