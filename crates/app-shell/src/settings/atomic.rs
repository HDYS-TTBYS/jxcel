//! 設定ファイルの原子的な書き込み（要件 7.1）。
//!
//! 同一ディレクトリの一時ファイルへ書き、`sync_all` で内容を永続化してから `rename` で置き換える。
//! truncate-then-write は使わない — 書き込みの途中でプロセスが落ちても、直前の完全な内容か
//! 新しい完全な内容のいずれかが残る（design.md「SettingsStore」の不変条件）。
//!
//! 本タスク（1.2）では骨組みのみを置く。実体は tasks.md 4.1 が追加する。
