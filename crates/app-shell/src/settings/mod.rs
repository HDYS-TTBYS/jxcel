//! 設定のキー空間・既定値・未知キーの保持・変更通知（要件 7.1〜7.7）。
//!
//! 設定ストアは自前で持つ（`tauri-plugin-store` は採らない。research.md 決定 6）。理由は、
//! プラグインの保存が truncate-then-write で原子的でないこと、未知キーの保持（要件 7.6）と
//! 破損時の既定値起動（要件 7.5）の意味論を持たないことである。全ウィンドウが同一の値を
//! 参照し（要件 7.3）、変更は購読側へ届く（要件 7.4）。保存できる値はシェルの設定に限り、
//! ドキュメントの内容を受け付けない（要件 7.7）。
//!
//! 原子的な書き込みの実体は [`atomic`] が持つ。本タスク（1.2）では骨組みのみを置く。
//! 実体は tasks.md 4.1（および 4.2、4.3）が追加する。

pub mod atomic;
