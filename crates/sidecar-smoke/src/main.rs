//! 補助プロセス機構の検証専用ヘルパー（tasks.md 1.6 が実体を追加する）。
//!
//! 親プロセスの識別子を受け取って監視し、親が消えたら自ら終了すること、および標準出力へ
//! 応答を返すことだけを検証する。実用的な機能は意図的に持たない（design.md
//! 「Out of Boundary」: 実物の言語サーバを登録するのは `macro-editor-lsp` である）。
//!
//! 本タスク（1.1）では、ワークスペースの members を解決させ `--all-targets` の
//! ビルドを成立させるための最小の `main` を置く。

fn main() {}
